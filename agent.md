# 项目协作规范

## 飞书文档维护

- 飞书知识库是策划文档的唯一权威来源。
- 不在项目目录或其他本地路径保存飞书文档正文、镜像、导出文件、快照或备份。
- 读取文档时直接通过飞书接口获取最新内容；需要处理正文时只在当前进程内存中使用，不落盘。
- 修改前重新读取线上最新版本并检查目标文档，修改后通过线上回读确认结果。
- 文档恢复依赖飞书自身的版本记录，不创建本地备份目录。

## 工具调用

### 飞书 / Lark

- 知识库、Docx 与多维表格维护默认使用 Lark MCP 的最小文档工具集，并以个人 OAuth 身份调用。该身份必须具备目标文档的读写权限；不得因方便而启用全量飞书 API。
- 启动 Lark MCP 前，在已配置的 MCP 工具目录运行 `npm run mcp:lark:refresh`。默认 `npm run mcp:lark` 也必须先执行该检查，并只在令牌剩余不足 15 分钟时刷新和轮换刷新凭证。
- 刷新命令提示缺少刷新凭证或刷新失败时，才运行 `npm run mcp:lark:login` 重新授权。重新登录后先确认已取得刷新凭证；不得在日志、文档、提交记录或对话中输出访问令牌、刷新凭证、应用密钥或其部分内容。
- 默认工具集只包含 Docx 正文和区块读取、创建、更新、删除，Wiki 节点读取与创建，以及多维表格记录读取、查询、创建、更新和批量更新。需要集合外接口时，先确认必要性，再在当前调用中显式增加该接口，不修改默认集合。
- Lark MCP 工具不能覆盖所需接口、协议行为与飞书 API 不一致，或必须排查底层权限时，才使用直接 OpenAPI。无论经由 MCP 还是 OpenAPI，均遵守“飞书文档维护”中的读前检查、写后回读和无本地正文规则。

### Godot MCP

- Godot MCP 用于启动编辑器、运行项目、读取调试输出、停止运行、查询项目与管理场景；不用于替代策划确认、代码审查或版本控制。
- 通过已配置的 Godot MCP 启动命令运行 `npm run mcp:godot`。其 `GODOT_PATH` 必须指向本机 Godot 4.6.2 可执行文件；目标目录必须包含 `project.godot`。
- 修改场景、脚本或资源后，优先使用 Godot MCP 运行对应项目并读取调试输出验证结果。运行中的项目需要停止后才能进行会影响场景或资源状态的操作。
- 使用场景创建、节点修改或资源保存工具前，先确认对应内容已由飞书策划定义；工具调用产生的临时输出不进入游戏运行目录。

## 文档解耦

- 每一项机制、流程、数值或内容表必须指定一个唯一的权威文档，并只在该文档中详细说明。
- 其他文档不重复规则；只有缺少该规则会使当前内容无法理解时，才以最短文字引用权威文档及对应章节。
- 与当前文档无直接关系的规则、输入、输出和链接直接不写，不为完整性补充背景说明。
- 新增规则前先确定归属文档；无法明确归属时，先整理职责边界，再写入正文。
- 修改已有规则时只以最小规模修改必要的权威文档，并检查必要引用、术语和链接是否仍然有效。
- 同一概念在所有文档中使用统一名称、ID 和单位。
- 如果发现重复内容，保留职责最匹配文档中的完整版本；其他位置直接删除，只有当前流程无法独立阅读时才保留简短引用。

## 逻辑完整性

- 系统设计必须尽可能覆盖完整的逻辑状态、状态转换和边界状态，不能只描述正常流程。
- 每个系统至少明确进入条件、进行中状态、完成条件、失败或取消条件，以及各状态之间的转换结果。
- 对并发触发、重复操作、资源不足、目标失效、系统耦合、阶段切换和回合结束等边界情况，必须在对应权威文档中给出处理规则。
- 边界规则直接写进相关流程或机制说明，不单独堆砌与系统无关的测试、验收或原型内容。
- 如果某个边界状态由其他系统负责，当前文档不重复定义；只有该状态会中断当前流程说明时，才简短引用其权威文档和章节。

## 策划文档写作格式

高优先级内容覆盖低优先级内容，自动修订不得覆盖更高优先级内容：

1. 人为直接修改的当前文档内容。
2. 人在对话框中的明确指示。
3. 当前模块的权威文档。
4. 当前正在修改的文档的既有内容。
5. 其他文档的非权威内容。

同一内容冲突时，以较高优先级为准；无法确认优先级或含义时不自动修订，标记待决定。

- 先用一段说明该节的显示或触发条件和整体规则，不直接罗列组件。
- 每一组规则单独成段。每段只解释一个判断、状态变化、限制或表现，避免把多个无关规则塞进同一段。
- 规则引出二级内容时，另起一个独立段落，再解释该二级内容。
- 一个内容包含多个并列组件时，用无序列表组织。每个列表项以“组件名：说明”开始，并写清其显示内容、位置、状态变化和限制。
- 一个内容包含先后步骤时，用有序列表组织。每一步明确触发条件、执行内容、成功结果，以及取消或失败时的处理。
- 使用飞书原生标题层级，不在标题文本中手写序号。
- 文档内涉及的所有关键词都需要详细解释。

## 最终规则过滤

- 只写负责人明确采纳的最终行为。
- 不写中间尝试、讨论理由、暂定结论、被否定方案或“禁止方案 A”。

## 开发准则

### 开发与文档对应

- 仅实现飞书策划已定义的内容。
- 文档未定义的功能、规则、数值或交互，先确认并更新文档，不自行假设。

### 简洁优先

- 用最少代码解决问题，拒绝冗余。
- 不为一次性需求引入抽象层或复杂架构。
- 代码可大幅精简时，主动重写。

### 精准修改

- 只修改与当前任务直接相关的代码。
- 不顺手调整相邻代码、注释或格式。
- 不重构正常运行的模块。

### 目标驱动执行

- 开始前定义可验证的成功标准。
- 修复 Bug：先用例复现，再调试至通过。
- 新增校验：为异常输入编写并通过测试。
- 重构：确保原有测试全部通过。
- 多步骤任务先给出简短计划及每步验证方式。

### 暴露问题优先

- 避免兜底逻辑，尽早暴露问题。
- 依靠清晰代码实现功能，不用兜底掩盖问题。
